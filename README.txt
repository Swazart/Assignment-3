In order to use the controls properly please read the following text.

Note that with each press of either rotate buttons the image rotation speed is increased. To reset this, clear the canvas then use the buttons again. 